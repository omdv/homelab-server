---
- name: start rancher container
  docker_container:
    name: rancher
    image: rancher/rancher
    state: started
    recreate: yes
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - "{{ docker_volumes }}/rancher:/var/lib/rancher"