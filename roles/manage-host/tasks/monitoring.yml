---
- name: install and configure smartmontools
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - smartmontools

- template:
    src: smartd.j2
    dest: /etc/smartd.conf
    owner: root
    group: root
    mode: "u=rw,g=r,o=r"