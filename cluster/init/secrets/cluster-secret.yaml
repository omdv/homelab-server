---
apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: cluster-external-secret
  namespace: external-secrets
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: cluster-secret
  data:
    - secretKey: domain
      remoteRef:
        key: secret/gitops
        property: VAULT_DOMAIN
    - secretKey: oauth_client_id
      remoteRef:
        key: secret/gitops
        property: VAULT_OAUTH_CLIENT_ID
    - secretKey: oauth_client_secret
      remoteRef:
        key: secret/gitops
        property: VAULT_OAUTH_CLIENT_SECRET
    - secretKey: oauth_email_whitelist
      remoteRef:
        key: secret/gitops
        property: VAULT_OAUTH_EMAIL_WHITELIST
