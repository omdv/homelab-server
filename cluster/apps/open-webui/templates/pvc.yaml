---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-open-webui
  labels:
    app: open-webui
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  volumeName: pv-open-webui
