---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-traefik
  labels:
    app: traefik

spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: pv-traefik
