---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-traefik
  labels:
    app: traefik

spec:
  capacity:
    storage: 1Gi
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /pool/config/traefik
