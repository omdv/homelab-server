---
apiVersion: v1
kind: Service
metadata:
  name: jupyter
  labels:
    app: jupyter

spec:
  ports:
    - protocol: TCP
      port: 8888
      targetPort: pod-custom-port
  selector:
    app: jupyter
