---
apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  labels:
    app: pgadmin

spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: pod-http-port
  selector:
    app: pgadmin
