---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: nginx
  namespace: default
  labels:
    app: nginx

spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.20-alpine
          ports:
            - name: web
              containerPort: 80
          volumeMounts:
            - mountPath: /usr/share/nginx/html
              name: storage-volume
          resources:
            requests:
              cpu: "100m"
              memory: "10Mi"
            limits:
              cpu: "300m"
              memory: "20Mi"
      volumes:
        - name: storage-volume
          persistentVolumeClaim:
            claimName: pvc-nginx
