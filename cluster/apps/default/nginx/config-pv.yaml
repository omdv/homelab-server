---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-nginx
  namespace: default
  labels:
    app: nginx

spec:
  capacity:
    storage: 10Mi
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /pool/config/nginx
