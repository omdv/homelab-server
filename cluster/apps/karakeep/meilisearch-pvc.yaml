---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-karakeep-meilisearch
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
  volumeName: pv-karakeep-meilisearch
