---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-karakeep-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: local-path
  volumeName: pv-karakeep-data
