---
apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: domain-external-secret
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: domain-secret
  data:
    - secretKey: domain
      remoteRef:
        key: secret/gitops
        property: VAULT_DOMAIN
