---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: traefik-forward-auth
  name: traefik-forward-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: traefik-forward-auth
  template:
    metadata:
      labels:
        app: traefik-forward-auth
    spec:
      containers:
        - name: traefik-forward-auth
          image: thomseddon/traefik-forward-auth:2
          ports:
            - containerPort: 4181
              protocol: TCP
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 256Mi
              cpu: "0.2"
          env:
            - name: PROVIDERS_GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: traefik-auth
                  key: OAUTH_CLIENT_ID
            - name: PROVIDERS_GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: traefik-auth
                  key: OAUTH_CLIENT_SECRET
            - name: SECRET
              valueFrom:
                secretKeyRef:
                  name: traefik-auth
                  key: OAUTH_SECRET
            - name: WHITELIST
              valueFrom:
                secretKeyRef:
                  name: traefik-auth
                  key: OAUTH_WHITELIST
            - name: AUTH_HOST
              value: auth.${HOMELAB_DOMAIN}
            - name: COOKIE_DOMAIN
              value: ${HOMELAB_DOMAIN}
            - name: INSECURE_COOKIE
              value: "false"
            - name: LIFETIME
              value: "2592000"
              # - name: LOG_LEVEL
              #   value: debug
