---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: nginx
  labels:
    app: nginx

spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`zzd.ai`)
      kind: Rule
      services:
        - name: nginx
          port: 80
  tls:
    certResolver: default
