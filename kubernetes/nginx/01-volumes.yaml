# ---
# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: pv-nginx
#   labels:
#     app: nginx

# spec:
#   capacity:
#     storage: 10Mi
#   storageClassName: longhorn
#   accessModes:
#     - ReadWriteOnce
#   hostPath:
#     path: /nginx-web/

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nginx
  labels:
    app: nginx

spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  volumeName: pv-nginx
