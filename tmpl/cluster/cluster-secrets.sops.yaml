# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
  SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
  SECRET_CERT_EMAIL: ${BOOTSTRAP_CERT_EMAIL}
  OAUTH_CLIENT_ID: ${HOMELAB_OAUTH_CLIENT_ID}
  OAUTH_CLIENT_SECRET: ${HOMELAB_OAUTH_CLIENT_SECRET}
  OAUTH_EMAIL_WHITELIST: ${HOMELAB_OAUTH_EMAIL_WHITELIST}
  OAUTH_PRODUCTION_SECRET: ${HOMELAB_OAUTH_PRODUCTION_SECRET}
