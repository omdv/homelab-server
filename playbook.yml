---
- name: -- SERVER SETUP --
  hosts: server
  roles:
    - setup-server
  vars:
    - user: om
      zfs_cache: /dev/sda4
      zfs_log: /dev/sda5
      zfs_pool: pool


- name: -- LXD SETUP--
  hosts: server
  roles:
    - setup-lxd

# - hosts: containers
#   tasks:
#     - name: Install vim
#       apt:
#         name: vim